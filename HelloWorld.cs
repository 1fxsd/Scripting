using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class qw : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        //Вывод приветствия игроку
        print("Hello, World!");
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
